# Generated from templates/radiography/radiography.rb.tmpl — do not edit directly.
# Run: gh workflow run bump-formula.yaml -f formula=radiography

class Radiography < Formula
  desc "CLI for scanning Android view hierarchies via Stoic"
  homepage "https://github.com/block/radiography"
  url "https://github.com/block/radiography/releases/download/{{.tag}}/radiography-stoic-plugin-{{.version}}.tar.gz"
  sha256 "{{ sha256 .assets "radiography-stoic-plugin" }}"
  license "Apache-2.0"
  version "{{.version}}"

  depends_on "block/tap/stoic"

  def install
    libexec.install Dir["*"]
    bin.install_symlink libexec/"radiography"
  end

  test do
    system "#{bin}/radiography", "--help"
  end
end
