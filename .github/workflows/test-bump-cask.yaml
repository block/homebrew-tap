name: Test Bump Cask

on:
  pull_request:
    paths:
      - "scripts/bump-cask.py"
      - "scripts/brew_utils.py"
      - "scripts/test_bump_cask.py"
      - "scripts/test_helpers.py"
      - ".github/workflows/test-bump-cask.yaml"
  push:
    branches:
      - main
    paths:
      - "scripts/bump-cask.py"
      - "scripts/brew_utils.py"
      - "scripts/test_bump_cask.py"
      - "scripts/test_helpers.py"
      - ".github/workflows/test-bump-cask.yaml"
  workflow_dispatch:

jobs:
  regression:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run bump-cask regression tests
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/test_bump_cask.py
